name: Upload Video to Catbox

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Video URL (TikTok, Instagram, Pinterest, etc.)'
        required: true
        type: string
      message_id:
        description: 'Discord Message ID for response tracking'
        required: false
        type: string

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install ffmpeg
        run: |
          sudo apt update
          sudo apt install -y ffmpeg

      - name: Install dependencies
        run: |
          pip install git+https://github.com/yt-dlp/yt-dlp.git
          pip install requests

      - name: Warm up Facebook link (if needed)
        if: contains(inputs.url, 'facebook.com') || contains(inputs.url, 'fb.watch')
        run: |
          echo "🔥 Прогрів Facebook посилання..."
          curl -s -L -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36" \
            --max-time 30 \
            "${{ inputs.url }}" > /dev/null 2>&1 || true
          sleep 3

      - name: Check content type and process
        id: content_processor
        run: |
          VIDEO_URL="${{ inputs.url }}"
          MESSAGE_ID="${{ inputs.message_id }}"
          
          echo "🔍 Перевірка типу контенту: $VIDEO_URL"
          
          # Отримуємо інформацію без завантаження
          INFO=$(yt-dlp --dump-json --simulate "$VIDEO_URL" 2>/dev/null) || INFO=""
          
          if [ -z "$INFO" ]; then
            echo "type=error" >> $GITHUB_OUTPUT
            if [ "${{ secrets.DISCORD_RESULTS_WEBHOOK }}" != "" ] && [ -n "$MESSAGE_ID" ]; then
              curl -X POST -H "Content-Type: application/json" \
                -d "{\"content\": \"❌ Не вдалося отримати інформацію про посилання для message $MESSAGE_ID\"}" \
                "${{ secrets.DISCORD_RESULTS_WEBHOOK }}"
            fi
            exit 0
          fi
          
          # Визначаємо тип контенту
          HAS_VIDEO=$(echo "$INFO" | python3 -c "
import sys, json
try:
    info = json.load(sys.stdin)
    formats = info.get('formats', [])
    if any(f.get('vcodec', 'none') != 'none' for f in formats):
        print('video')
    elif any(f.get('acodec', 'none') != 'none' for f in formats):
        print('audio')
    elif info.get('thumbnail'):
        print('image')
    else:
        print('text')
except:
    print('unknown')
")
          
          echo "type=$HAS_VIDEO" >> $GITHUB_OUTPUT
          TITLE=$(echo "$INFO" | python3 -c "import sys, json; print(json.load(sys.stdin).get('title', 'Без назви'))")
          echo "title=$TITLE" >> $GITHUB_OUTPUT
          
          # Якщо це відео — продовжуємо
          if [ "$HAS_VIDEO" = "video" ]; then
            echo "processing=video" >> $GITHUB_OUTPUT
          else
            # Для інших типів — повідомляємо
            case "$HAS_VIDEO" in
              "image") CONTENT="🖼️ Картинка" ;;
              "audio") CONTENT="🎵 Аудіо" ;;
              "text") CONTENT="📝 Текстовий пост" ;;
              *) CONTENT="📄 Невідомий тип" ;;
            esac
            
            if [ "${{ secrets.DISCORD_RESULTS_WEBHOOK }}" != "" ] && [ -n "$MESSAGE_ID" ]; then
              curl -X POST -H "Content-Type: application/json" \
                -d "{\"content\": \"$CONTENT для message $MESSAGE_ID\"}" \
                "${{ secrets.DISCORD_RESULTS_WEBHOOK }}"
            fi
            echo "processing=skip" >> $GITHUB_OUTPUT
            exit 0
          fi

      - name: Check and upload video
        if: steps.content_processor.outputs.processing == 'video'
        id: catbox
        run: |
          VIDEO_URL="${{ inputs.url }}"
          MESSAGE_ID="${{ inputs.message_id }}"
          MAX_SIZE_BYTES=$((180 * 1024 * 1024))

          echo "📥 Завантаження та вивантаження на Catbox..."
          
          OUTPUT_LINK=$(yt-dlp \
            --retries 3 \
            --fragment-retries 3 \
            --sleep-interval 2 \
            --merge-output-format mp4 \
            --allow-unplayable-formats \
            --extractor-args "pinterest:use_webpage=True" \
            --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36" \
            --no-check-certificate \
            --output - \
            "$VIDEO_URL" | \
            curl -s -H "Expect:" \
            -F "reqtype=fileupload" \
            -F "fileToUpload=@-;filename=video.mp4;type=video/mp4" \
            https://catbox.moe/user/api.php)

          if [[ "$OUTPUT_LINK" =~ ^https:// ]]; then
            echo "✅ Успішно: $OUTPUT_LINK"
            echo "link=$OUTPUT_LINK" >> $GITHUB_OUTPUT
          else
            echo "❌ Помилка вивантаження: $OUTPUT_LINK"
            if [ "${{ secrets.DISCORD_RESULTS_WEBHOOK }}" != "" ] && [ -n "$MESSAGE_ID" ]; then
              curl -X POST -H "Content-Type: application/json" \
                -d "{\"content\": \"❌ Не вдалося вивантажити на Catbox для message $MESSAGE_ID\"}" \
                "${{ secrets.DISCORD_RESULTS_WEBHOOK }}"
            fi
            exit 1
          fi

      - name: Send result to results channel
        if: success() && steps.content_processor.outputs.processing == 'video'
        run: |
          MESSAGE_ID="${{ inputs.message_id }}"
          if [ "${{ secrets.DISCORD_RESULTS_WEBHOOK }}" != "" ] && [ -n "$MESSAGE_ID" ]; then
            curl -X POST -H "Content-Type: application/json" \
            -d "{\"content\": \"🔗 Result for message $MESSAGE_ID: ${{ steps.catbox.outputs.link }}\"}" \
            "${{ secrets.DISCORD_RESULTS_WEBHOOK }}"
          fi
